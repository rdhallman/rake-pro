require "bundler/gem_tasks"
require "rspec/core/rake_task"

RSpec::Core::RakeTask.new(:spec)

task :default => :spec

# rake build install
# git remote add origin https://github.com/dhallman/rake-pro.git
# git commit -m "Packing my rake extensions as a gem so they can be easily imported into any project"
# rake release
